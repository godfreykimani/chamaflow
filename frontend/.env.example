# ── ChamaFlow Frontend Environment ─────────────────────────────────────────────
# Copy to .env.local for local dev, set in Vercel dashboard for production

# Leave empty in dev — Vite proxy handles /api → localhost:3001
# Set to your Railway backend URL in production:
VITE_API_URL=https://chamaflow-api.up.railway.app/api
